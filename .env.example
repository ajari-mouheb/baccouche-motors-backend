DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=your_password
DB_NAME=baccouchemotors

JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES=7d

# Optional: Restrict CORS to frontend origin (e.g. http://localhost:3001)
# FRONTEND_URL=http://localhost:3001

# Optional: SMTP for email (password reset, test drive confirmation, contact confirmation)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM=noreply@baccouche-motors.com
